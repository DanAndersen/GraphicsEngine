#include <stdio.h>

#include "dsard_hello_world.h"
namespace dsard {
	void HelloWorld::init() {
		//fprintf(stdout, "HelloWorld::init()\n");
	}

	void HelloWorld::draw() {
		//fprintf(stdout, "HelloWorld::draw()\n");
	}
}